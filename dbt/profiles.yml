social_media_analytics:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: airflow
      password: airflow
      port: 5432
      dbname: analytics_db
      schema: public
      threads: 1
      keepalives_idle: 0
      sslmode: disable
